# Копируй в `.env` и при необходимости меняй.
# Vite отдаёт в клиент только переменные с префиксом VITE_.

# База API (без завершающего /). Локально часто http://localhost:8082. Пусто = дефолт в коде (Railway).
VITE_BACK_API=http://localhost:8082
